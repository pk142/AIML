# ── Core API ───────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
pydantic==2.7.1
pydantic-settings==2.3.1

# ── Document processing ────────────────────────────────────────────────────
pymupdf==1.24.5          # PDF extraction with page numbers
python-docx==1.1.2       # DOCX parsing
chardet==5.2.0           # TXT encoding detection

# ── Vector DB (in-memory mode, no Docker needed) ───────────────────────────
qdrant-client==1.9.1

# ── DEV MODE embeddings (replaces Ollama + nomic-embed-text) ──────────────
sentence-transformers==3.0.1   # all-MiniLM-L6-v2 → 384-dim, ~80 MB model
torch==2.3.1                   # CPU-only inference, ~500 MB install

# ── HTTP client (used only in production mode for real Ollama) ─────────────
httpx==0.27.0

# ── Streamlit frontend ─────────────────────────────────────────────────────
streamlit==1.35.0
requests==2.32.3
